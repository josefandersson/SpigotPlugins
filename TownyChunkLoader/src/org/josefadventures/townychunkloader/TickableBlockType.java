package org.josefadventures.townychunkloader;

public enum TickableBlockType {

    SAPLING, // all saplings
    CROP, // wheat, melon seed/stem, pumpkin seed/stem, potato, carrot,
    CROP_NETHER, // nether warts (they are also Crops, but have an age of 4 instead of 8)
    SUGAR_CANE, // sugar cane
    CACTI // cacti

    // mushrooms
    // vines

    // chorus? sea pickle? coral?

}
